<%!
	import json, random
%>
<%
colors = [
	  "#FEF0D9"
	, "#FDCC8A"
	, "#FC8D59"
	, "#E34A33"
	, "#B30000"
]
data = json.load(open("/home/niles/ShowStreets/resources/" + d["file"]))
for street in data["features"]:
	#if street["properties"]["type"] == "sidewalk": continue
	street["properties"]["stroke"] = colors[random.randint(0, len(colors)-1)]
%>
${json.dumps(data)}
